import _objectSpread from '@babel/runtime/helpers/esm/objectSpread2';
import _objectWithoutProperties from '@babel/runtime/helpers/esm/objectWithoutProperties';
import path from 'path';
import { CloudinaryImage } from '@keystonejs/fields-cloudinary-image';
import { CloudinaryAdapter } from '@keystonejs/file-adapters';

const cloudinaryImage = (_ref) => {
  let {
    cloudinary
  } = _ref,
      config = _objectWithoutProperties(_ref, ["cloudinary"]);

  return {
    type: CloudinaryImage,
    config: _objectSpread(_objectSpread({}, config), {}, {
      // @ts-ignore
      adapter: new CloudinaryAdapter(cloudinary)
    }),
    views: path.join(path.dirname(require.resolve('@keystone-next/cloudinary/package.json')), 'views')
  };
};

export { cloudinaryImage };

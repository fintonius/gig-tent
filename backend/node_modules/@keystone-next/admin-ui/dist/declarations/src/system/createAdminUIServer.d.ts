import express from 'express';
import type { KeystoneConfig, SessionImplementation, CreateContext } from '@keystone-next/types';
export declare const createAdminUIServer: (ui: KeystoneConfig['ui'], createContext: CreateContext, dev: boolean, projectAdminPath: string, sessionImplementation?: SessionImplementation | undefined) => Promise<(req: express.Request, res: express.Response) => Promise<void>>;

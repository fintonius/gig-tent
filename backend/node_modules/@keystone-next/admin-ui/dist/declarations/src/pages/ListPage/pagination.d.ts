import { jsx } from '@keystone-ui/core';
export declare function Pagination({ currentPage, total, pageSize, listKey, }: {
    currentPage: number;
    total: number;
    pageSize: number;
    listKey: string;
}): jsx.JSX.Element | null;
export declare function PaginationLabel({ currentPage, pageSize, plural, singular, total, }: {
    currentPage: number;
    pageSize: number;
    plural: string;
    singular: string;
    total: number;
}): jsx.JSX.Element;

import { JSONValue, ListMeta } from '@keystone-next/types';
export declare type Filter = {
    field: string;
    type: string;
    value: JSONValue;
};
export declare function useFilters(list: ListMeta): {
    filters: Filter[];
    where: {};
};

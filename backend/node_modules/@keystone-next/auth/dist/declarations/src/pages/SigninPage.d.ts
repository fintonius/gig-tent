import { jsx } from '@keystone-ui/core';
export declare const SigninPage: ({ identityField, secretField, mutationName, successTypename, failureTypename, }: {
    identityField: string;
    secretField: string;
    mutationName: string;
    successTypename: string;
    failureTypename: string;
}) => jsx.JSX.Element;

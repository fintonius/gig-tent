import type { KeystoneListsAPI } from '@keystone-next/types';
import { AuthTokenRequestErrorCode } from '../types';
export declare function updateAuthToken(identityField: string, protectIdentities: boolean, identity: string, itemAPI: KeystoneListsAPI<any>[string]): Promise<{
    success: false;
    code?: AuthTokenRequestErrorCode;
} | {
    success: true;
    itemId: string | number;
    token: string;
}>;

import type { KeystoneListsAPI } from '@keystone-next/types';
import { PasswordAuthErrorCode } from '../types';
export declare function validateSecret(list: any, identityField: string, identity: string, secretField: string, protectIdentities: boolean, secret: string, itemAPI: KeystoneListsAPI<any>[string]): Promise<{
    success: false;
    code: PasswordAuthErrorCode;
} | {
    success: true;
    item: {
        id: any;
        [prop: string]: any;
    };
}>;

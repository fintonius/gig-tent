import type { KeystoneListsAPI } from '@keystone-next/types';
import { AuthTokenRedemptionErrorCode } from '../types';
export declare function validateAuthToken(tokenType: 'passwordReset' | 'magicAuth', list: any, identityField: string, identity: string, protectIdentities: boolean, tokenValidMins: number | undefined, token: string, itemAPI: KeystoneListsAPI<any>[string]): Promise<{
    success: false;
    code: AuthTokenRedemptionErrorCode;
} | {
    success: true;
    item: {
        id: any;
        [prop: string]: any;
    };
}>;

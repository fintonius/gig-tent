import { AuthTokenRedemptionErrorCode, AuthTokenRequestErrorCode, PasswordAuthErrorCode } from '../types';
export declare function getPasswordAuthError({ identityField, secretField, code, itemPlural, itemSingular, }: {
    identityField: string;
    secretField: string;
    itemSingular: string;
    itemPlural: string;
    code: PasswordAuthErrorCode;
}): string;
export declare function getAuthTokenErrorMessage({ identityField, code, itemPlural, itemSingular, }: {
    identityField: string;
    itemSingular: string;
    itemPlural: string;
    code: AuthTokenRedemptionErrorCode | AuthTokenRequestErrorCode;
}): string;

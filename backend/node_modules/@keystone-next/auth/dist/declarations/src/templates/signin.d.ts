import { AuthGqlNames } from '../types';
export declare const signinTemplate: ({ gqlNames, identityField, secretField, }: {
    gqlNames: AuthGqlNames;
    identityField: string;
    secretField: string;
}) => string;

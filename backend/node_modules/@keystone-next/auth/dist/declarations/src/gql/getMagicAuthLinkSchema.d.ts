import type { GraphQLSchemaExtension } from '@keystone-next/types';
import { AuthGqlNames, AuthTokenTypeConfig } from '../types';
export declare function getMagicAuthLinkSchema<I extends string>({ listKey, identityField, protectIdentities, gqlNames, magicAuthLink, }: {
    listKey: string;
    identityField: I;
    protectIdentities: boolean;
    gqlNames: AuthGqlNames;
    magicAuthLink: AuthTokenTypeConfig;
}): GraphQLSchemaExtension;

import { ReactNode } from 'react';
import { jsx } from '@keystone-ui/core';
declare type SigninContainerProps = {
    children: ReactNode;
};
export declare const SigninContainer: ({ children }: SigninContainerProps) => jsx.JSX.Element;
export {};

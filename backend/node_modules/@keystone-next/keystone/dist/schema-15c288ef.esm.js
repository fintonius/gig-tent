import { mergeSchemas } from '@graphql-tools/merge';

function createSchema(config) {
  return config;
}
function config(config) {
  return config;
}
function list(config) {
  return config;
}
function singleton(config) {
  return config;
}
function gql(strings) {
  return strings[0];
}
function graphQLSchemaExtension({
  typeDefs,
  resolvers
}) {
  return schema => mergeSchemas({
    schemas: [schema],
    typeDefs,
    resolvers
  });
}

export { graphQLSchemaExtension as a, config as b, createSchema as c, gql as g, list as l, singleton as s };

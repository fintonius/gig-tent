var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import { getCaptionProps } from './getCaptionProps';
import { defaultProps } from '../DayPicker/defaults/defaultProps';
describe('getCaptionProps', function () {
    it('return the container props', function () {
        var classNames = {
            caption: 'caption-foo'
        };
        var props = __assign(__assign({}, defaultProps), { classNames: classNames });
        var expected = {
            containerProps: {
                className: 'caption-foo'
            }
        };
        var result = getCaptionProps(props);
        expect(result).toMatchObject(expected);
    });
});
//# sourceMappingURL=getCaptionProps.test.js.map
import { DayModifier } from '../../components/DayPicker';
/**
 * Return the modifier for days outside the current month.
 */
export declare function getOutsideModifier(day: Date, currentMonth: Date): DayModifier | undefined;
